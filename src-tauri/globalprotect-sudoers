# The helper accepts only a portal and username and invokes OpenConnect with
# the fixed GlobalProtect arguments. It must remain root-owned and mode 0755.
ALL ALL=(root) NOPASSWD: /usr/libexec/globalprotect/openconnect-helper
